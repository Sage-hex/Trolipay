# Copy this file to .env for local development/testing.
# Loading order in app config is: .env -> .env.local -> .env.example.
# That means if you later delete .env.example and keep only .env, there is no conflict.

# App runtime DB
DATABASE_URL=sqlite:///./app.db

# Optional test DB for pytest.
# If TEST_DATABASE_URL is set (like below), tests use that real DB.
# If you clear TEST_DATABASE_URL, tests fall back to in-memory SQLite.
TEST_DATABASE_URL=postgresql://neondb_owner:npg_9krzNKWF6Aaw@ep-wild-waterfall-absuneld-pooler.eu-west-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require

JWT_SECRET=change-me
PAYSTACK_SECRET_KEY=
PAYSTACK_WEBHOOK_TOKEN=
TELEGRAM_WEBHOOK_TOKEN=
WHATSAPP_VERIFY_TOKEN=
WHATSAPP_APP_SECRET=
MEDIA_DIR=media
